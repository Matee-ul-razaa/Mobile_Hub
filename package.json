{
  "name": "mobile-hub-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npx vite build && cp -r dist ../dist",
    "vercel-build": "npm run build"
  }
}
